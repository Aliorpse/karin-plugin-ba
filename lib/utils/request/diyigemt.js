import { logger } from "node-karin"

const BASE_URL = 'https://arona.diyigemt.com/api/v2'
const BASE_URL_CDN = 'https://arona.cdn.diyigemt.com/image'

const APIS = {
}

const get_diyigemt = async (api, data) => {
}

export { get_diyigemt }